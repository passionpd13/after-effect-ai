{
  "name": "after-effect-ai",
  "private": true,
  "scripts": {
    "install": "cd web && npm install",
    "build": "cd web && npm run build",
    "start": "cd web && npx next start -H 0.0.0.0 -p ${PORT:-3000}"
  }
}
