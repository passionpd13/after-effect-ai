{
  "name": "after-effect-ai",
  "private": true,
  "scripts": {
    "install": "cd web && npm install",
    "build": "cd web && npm run build && cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public",
    "start": "HOSTNAME=0.0.0.0 PORT=${PORT:-3000} node web/.next/standalone/server.js"
  }
}
