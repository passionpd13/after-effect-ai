[build]
builder = "NIXPACKS"

[build.nixpacks]
nixPkgsArchive = "github:NixOS/nixpkgs/nixos-24.11"

[deploy]
startCommand = "cd web && node .next/standalone/server.js"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
